<%= erb :'shared/header', :locals => locals %>

<%# 'the title is: ' + locals[:title] %>
<%= 'the title is: ' + title %>

  <article class="row span10 offset1">
    <%= yield %>
  </article>
  
  <% if File.exist?("views/asides/#{category}/#{name}.md") %>
  <aside class="row span3">
    <%= markdown "asides/#{category}/#{name}".to_sym %>
  </aside>
  <% end %>

<%= erb :'shared/footer', :locals => locals %>
